{
  "name": "playwright-basics",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:verify": "npx playwright test --headed",
    "test:browsers": "npx playwright test --browser=all",
    "test:initial": "npx playwright test --grep @initial",
    "test:report": "npx playwright test --config=e2e.config.ts --reporter=html && npx playwright show-report",
    "test:e2e": "npx playwright test --config=e2e.config.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.31.2",
    "prettier": "^2.8.4"
  }
}
